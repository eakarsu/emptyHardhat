# Deployment evidence

No live deployment is checked in or implied. For each approved network, store the JSON emitted by `scripts/deploy.js` in the organization's immutable release system together with the Git commit, compiler/settings, artifact hash, transaction hash/receipt, verified source link, administrator custody record, tenant registration approvals, independent security review, and rollback/migration plan. Do not commit private keys or secret RPC URLs.
